// Re-export everything from the Convex version for backward compatibility
export {
  useSettingsConvex as useSettings,
  AI_MODELS,
  type AIProvider,
  type AIModel,
  type AIProviderConfig,
  type AppSettings,
} from './convex/useSettingsConvex';
